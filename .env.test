# Committed on purpose: Vitest runs in mode "test", so Vite loads this file automatically and
# config/env.ts can validate successfully in CI, which has no .env. Not a real key.
VITE_BASE_URL="https://api.openweathermap.org/"
VITE_API_KEY="test-api-key"
