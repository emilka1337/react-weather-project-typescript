# Copy to .env and fill in. Vite inlines every VITE_* variable into the client bundle,
# so treat these as public values, not as secrets.

# Trailing slash is required: paths are appended as `${VITE_BASE_URL}data/2.5/forecast`
VITE_BASE_URL="https://api.openweathermap.org/"
VITE_API_KEY=""
